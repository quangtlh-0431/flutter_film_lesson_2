targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: flutter_basic_lesson_2|lib/graphQL/schema.graphql
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: flutter_basic_lesson_2|lib/graphQL/schema.graphql